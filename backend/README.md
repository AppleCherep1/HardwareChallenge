# 🤖 Virtual Receptionist AI Assistant (Hardware Challenge)

Интерактивный ИИ-ассистент для роли виртуального ресепшиониста.  
Ассистент **видит человека через камеру**, **реагирует на появление**, **общается голосом** и **отображает анимированного аватара** в браузере.

Проект реализован в рамках **Hardware Challenge / AI-конкурса**.

---

## 🎯 Идея проекта

Создать виртуального ресепшиониста, который:
- автоматически определяет появление человека перед камерой;
- приветствует посетителя **без нажатия кнопок**;
- ведёт диалог с помощью LLM;
- озвучивает ответы приятным голосом;
- визуально отображается как «живой» аватар.

---

## 🧠 Архитектура системы
Камера (DroidCam / Webcam)
↓
YOLOv5 (детекция человека)
↓
Логика состояния (idle / active)
↓
LLM (диалог)
↓
TTS (Web Speech API)
↓
UI с анимированным аватаром



---

## ⚙️ Используемые технологии

### Backend
- **Python 3.11**
- **FastAPI**
- **OpenCV**
- **YOLOv5 (ONNX через OpenCV DNN)**
- **Whisper (STT, локально)**
- **LLM через OpenRouter / OpenAI-совместимый API**

### Frontend (UI)
- **HTML / CSS / JavaScript**
- **Web Speech API (Text-to-Speech)** — полностью бесплатно
- PNG-аватар с состояниями:
  - `idle`
  - `talk`

### Hardware
- Ноутбук (CPU + GPU)
- Камера через **DroidCam (смартфон)**

---

## ✅ Выполнение требований ТЗ

✔ Детекция присутствия человека  
✔ Реакция ассистента на появление  
✔ Голосовой вывод (TTS)  
✔ Интерактивный диалог  
✔ Визуальный аватар  
✔ Локальный запуск без облачных камер  
✔ Безопасная работа с API-ключами (`.env` не в репозитории)

---

## 🚀 Как запустить проект локально

### 1️⃣ Клонировать репозиторий
```bash
git clone https://github.com/AppleCherep1/HardwareChallenge.git
cd HardwareChallenge

python -m venv venv
venv\Scripts\activate


pip install -r requirements.txt

OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=mistralai/mistral-7b-instruct

cd backend
python -m uvicorn app:app --reload


